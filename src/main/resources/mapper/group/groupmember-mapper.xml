<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.beyond.beatbuddy.group.mapper.GroupMemberMapper">

    <resultMap id="GroupMemberResultMap" type="GroupMember">
        <id property="memberId" column="member_id"/>
        <result property="groupId" column="group_id"/>
        <result property="userId" column="user_id"/>
        <result property="groupNickname" column="group_nickname"/>
        <result property="joinedAt" column="joined_at"/>
    </resultMap>

    <select id="existsByGroupIdAndUserId" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM group_members
        WHERE group_id = #{groupId}
          AND user_id = #{userId}
    </select>

    <select id="existsByGroupIdAndGroupNickname" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM group_members
        WHERE group_id = #{groupId}
          AND group_nickname = #{groupNickname}
    </select>

    <insert id="save" useGeneratedKeys="true" keyProperty="memberId">
        INSERT INTO group_members (
            group_id, user_id, group_nickname
        ) VALUES (
                     #{groupId}, #{userId}, #{groupNickname}
                 )
    </insert>

    <delete id="deleteByGroupIdAndUserId">
        DELETE FROM group_members
        WHERE group_id = #{groupId}
          AND user_id = #{userId}
    </delete>

</mapper>